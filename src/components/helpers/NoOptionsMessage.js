import React from 'react';
import { components } from 'react-select';

export const NoOptionsMessage = (props, context) => (
  <components.NoOptionsMessage>
    {context}
  </components.NoOptionsMessage>
); 
